What happens when you initialize a repository? Why do you need to do it?
Initialising created meta data about the folder, like how many untracked files are present and all


How is the staging area different from the working directory and the repository? What value do you think it offers?
Staging area acts like a bridge between working directory and the repository.......It customizes the part of the commit we need to make like just adding some files to commit and not all in the repository


How can you use the staging area to make sure you have one commit per logical change?
Even though if we have multiple logics to update across different files, using staging are we can commit only one logic at once


What are some situations when branches would be helpful in keeping your history organized? How would branches help?
when we want to experiment with the code or made some changes and make new thing of the master then in these conditions branches would be useful


How do the diagrams help you visualize the branch structure?
Diagrams help us to see different brahces presesnt and help us to visualise the branch structure


What is the result of merging two branches together? Why do we represent it in the diagram the way we do?
merging makes two different braches to merge and allows us to have both the functionalities in the final merged code

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?
Pros : It makes work easy, saves time, not need to go thorugh entire code, easy to differentiate two sets of code if there is a conflict
Cons : When merging happens but in wrong way